<<<<<<< HEAD
﻿@model MyCompany.MyCustomModule.ViewModels.MyCustomPartViewModel
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer T
@if (!string.IsNullOrWhiteSpace(Model.Summary))
{
    <div class="mycustom-part-summary">
        @if (Model.IsFeatured)
        {
            <span class="featured-icon" title="@T["Featured"]">
                <i class="fas fa-star text-warning"></i>
            </span>
        }
        
        <div class="summary-text">
            @{
                var truncatedSummary = Model.Summary.Length > 150 
                    ? Model.Summary.Substring(0, 150) + "..." 
                    : Model.Summary;
            }
            <p>@truncatedSummary</p>
        </div>
        
        @if (Model.Priority >= 4)
        {
            <span class="priority-icon" title="@T["High Priority"]">
                <i class="fas fa-exclamation-triangle text-warning"></i>
            </span>
        }
    </div>
}

<style>
    .mycustom-part-summary {
        display: flex;
        align-items: flex-start;
        gap: 0.5rem;
        padding: 0.75rem;
        background-color: #f8f9fa;
        border-left: 4px solid #007bff;
        margin: 0.5rem 0;
    }
    
    .featured-icon, .priority-icon {
        flex-shrink: 0;
        margin-top: 0.25rem;
    }
    
    .summary-text {
        flex: 1;
    }
    
    .summary-text p {
        margin: 0;
        color: #6c757d;
        font-size: 0.9rem;
        line-height: 1.4;
    }
=======
﻿@model MyCompany.MyCustomModule.ViewModels.MyCustomPartViewModel
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer T
@if (!string.IsNullOrWhiteSpace(Model.Summary))
{
    <div class="mycustom-part-summary">
        @if (Model.IsFeatured)
        {
            <span class="featured-icon" title="@T["Featured"]">
                <i class="fas fa-star text-warning"></i>
            </span>
        }
        
        <div class="summary-text">
            @{
                var truncatedSummary = Model.Summary.Length > 150 
                    ? Model.Summary.Substring(0, 150) + "..." 
                    : Model.Summary;
            }
            <p>@truncatedSummary</p>
        </div>
        
        @if (Model.Priority >= 4)
        {
            <span class="priority-icon" title="@T["High Priority"]">
                <i class="fas fa-exclamation-triangle text-warning"></i>
            </span>
        }
    </div>
}

<style>
    .mycustom-part-summary {
        display: flex;
        align-items: flex-start;
        gap: 0.5rem;
        padding: 0.75rem;
        background-color: #f8f9fa;
        border-left: 4px solid #007bff;
        margin: 0.5rem 0;
    }
    
    .featured-icon, .priority-icon {
        flex-shrink: 0;
        margin-top: 0.25rem;
    }
    
    .summary-text {
        flex: 1;
    }
    
    .summary-text p {
        margin: 0;
        color: #6c757d;
        font-size: 0.9rem;
        line-height: 1.4;
    }
>>>>>>> 5cde5b2 (Add Drivers and Models to version control)
</style>